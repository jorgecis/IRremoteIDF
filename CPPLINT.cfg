set noparent
linelength=80
# Namespaces in this code base are not indented (upstream convention).
# ESP-IDF's own headers are included with quotes and no directory prefix.
filter=-whitespace/indent_namespace,-build/include_subdir
